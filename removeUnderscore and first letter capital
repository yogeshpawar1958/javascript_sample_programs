let str="yogesh_bhausaheb_pawar";
let result=''
for(let data of str){
    if(data=='_'){
        result+=' '
    }else {
         result+=data
    }
}
out = result.split(" ").map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(" ");
console.log(out)
